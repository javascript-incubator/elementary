import { breakpoints, fontSizes, space } from './constants'

export const weights = [
  400,
  700
]

export const colors = {
  black: '#000',
  white: '#fff'
}

export const font = `-apple-system, BlinkMacSystemFont, sans-serif`
export const monospace = '"SF Mono", "Roboto Mono", Menlo, monospace'

export default {
  breakpoints,
  space,
  fontSizes,
  weights,
  font,
  monospace,
  colors
}
